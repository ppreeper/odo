# odo

keep the bash
add the odo_kube.py to accomodate the yaml portions

```
.
├── .config
│   └── oda
│       └── manifests
├── .local
│   └── oda
└── workspace
    ├── odoo
    └── repos
        └── odoo
```
